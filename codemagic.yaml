workflows:
  ios_poc:
    name: "iOS TryggSamtal PoC"
    environment:
      xcode: latest
      groups:
        - admin
      vars:
        BUNDLE_ID: "com.yourname.tryggsamtal"
        TEAM_ID: "93397B64RG"
        XCODE_PROJECT: "TryggSamtal.xcodeproj"
        XCODE_SCHEME: "TryggSamtal"
        IPA_NAME: "TryggSamtal"
        CODESIGN_ID: "Apple Development"
        PROFILE_PATH: "profiles/TryggsamtalDev.mobileprovision"
        KEYCHAIN_PATH: "$HOME/Library/Keychains/build.keychain-db"
        # Fyll i om din org kräver provider vid uppladdning (valfritt):
        # ASC_PROVIDER: "YourProviderShortName"

    scripts:
      # 1) Verktyg
      - name: Install tools
        script: |
          set -e
          echo ">>> Installing tools"
          brew update || true
          brew install xcodegen xcbeautify openssl || true
          xcodegen --version || true
          xcbeautify --version || true
          /usr/bin/openssl version || true

      # 2) Skapa .xcodeproj från project.yml
      - name: Generate Xcode project via XcodeGen
        script: |
          set -e
          echo "PWD:" && pwd
          echo ">>> Repo tree (top):"
          ls -la
          if [ -f project.yml ]; then
            echo ">>> Running xcodegen generate"
            xcodegen generate
          else
            echo "❌ Missing project.yml file"; exit 1
          fi
          test -f "TryggSamtal.xcodeproj/project.pbxproj" || { echo "❌ XcodeGen failed"; exit 1; }
          echo ">>> .xcodeproj created OK"

      # 3) Kontrollera p12-blob från Secure env
      - name: Diagnose p12 env payload
        script: |
          set -e
          echo ">>> Validating CERT_P12_B64 and CERT_P12_PASSWORD..."
          if [ -z "$CERT_P12_B64" ] || [ -z "$CERT_P12_PASSWORD" ]; then
            echo "❌ Missing CERT_P12_B64 or CERT_P12_PASSWORD"; exit 1
          fi
          echo "$CERT_P12_B64" | base64 --decode > check.p12
          echo ">>> File type:" && file check.p12 || true
          echo ">>> OpenSSL read test:"
          if ! (openssl pkcs12 -in check.p12 -nokeys -passin pass:"$CERT_P12_PASSWORD" -info -clcerts >/dev/null 2>&1); then
            echo "❌ Wrong password OR corrupted p12"; exit 1
          fi
          echo "✅ PKCS#12 looks valid"

      # 4) Skapa keychain
      - name: Create and configure keychain (no partition-list yet)
        script: |
          set -e
          echo ">>> Creating keychain at $KEYCHAIN_PATH"
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"
          echo "✅ Keychain created & set default"

      # 5) Importera p12 (robust)
      - name: Inject Apple Development .p12 into keychain (robust, no heredocs)
        script: |
          set -e
          echo ">>> Importing Apple Development certificate..."
          echo "$CERT_P12_B64" | base64 --decode > ios_dev.p12

          if security import ios_dev.p12 -k "$KEYCHAIN_PATH" -P "$CERT_P12_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security; then
            echo "✅ Direct security import succeeded"
          else
            echo "⚠️ Direct import failed; extracting cert+key PEM and importing separately..."
            openssl pkcs12 -in ios_dev.p12 -passin pass:"$CERT_P12_PASSWORD" -nodes -clcerts -out extracted.pem
            awk 'BEGIN{p=0} /BEGIN CERTIFICATE/{p=1} p{print} /END CERTIFICATE/{p=0}' extracted.pem > dev_cert.pem
            awk 'BEGIN{p=0} /BEGIN PRIVATE KEY/{p=1} p{print} /END PRIVATE KEY/{p=0}' extracted.pem > dev_key.pem
            security import dev_cert.pem -k "$KEYCHAIN_PATH" -T /usr/bin/codesign -T /usr/bin/security
            security import dev_key.pem  -k "$KEYCHAIN_PATH" -P "" -A -t private -f pem || \
            security import dev_key.pem  -k "$KEYCHAIN_PATH" -P "" -A -T /usr/bin/codesign -T /usr/bin/security
            rm -f extracted.pem dev_cert.pem dev_key.pem
            echo "✅ Imported certificate + private key separately"
          fi

          echo ">>> Setting key partition list…"
          security set-key-partition-list -S apple-tool:,apple: -s -k "" "$KEYCHAIN_PATH" || true

          echo ">>> Codesigning identities in keychain:"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH" || true
          if ! security find-identity -v -p codesigning "$KEYCHAIN_PATH" | grep -F "Apple Development" >/dev/null; then
            echo "❌ No 'Apple Development' identity found after import."; exit 1
          fi
          echo "✅ Apple Development identity present"

      # 6) Installera provisioning profile och plocka ut UUID
      - name: Install provisioning profile (extract UUID)
        script: |
          set -e
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          if [ ! -f "$PROFILE_PATH" ]; then
            echo "❌ Provisioning profile not found at $PROFILE_PATH"; exit 1
          fi
          echo "Found profile: $PROFILE_PATH"
          PLIST=$(security cms -D -i "$PROFILE_PATH")
          echo ">>> First lines of decoded .mobileprovision:"
          echo "$PLIST" | head -n 25 || true
          UUID=$(echo "$PLIST" | plutil -extract UUID raw -)
          NAME=$(echo "$PLIST" | plutil -extract Name raw - 2>/dev/null || true)
          APPID=$(echo "$PLIST" | plutil -extract Entitlements.application-identifier raw - 2>/dev/null || true)
          TEAM_IN_PROF=$(echo "$APPID" | awk -F'.' '{print $1}')
          echo "Profile Name: ${NAME:-<unknown>}"
          echo "Profile UUID: $UUID"
          echo "application-identifier: ${APPID:-<unknown>} (expected ${TEAM_ID}.${BUNDLE_ID})"
          [ "$TEAM_IN_PROF" = "$TEAM_ID" ] || echo "⚠️ Team ID in profile differs from TEAM_ID"
          cp "$PROFILE_PATH" "$HOME/Library/MobileDevice/Provisioning Profiles/$UUID.mobileprovision"
          echo "PROVISIONING_PROFILE_UUID=$UUID" >> $CM_ENV
          ls -la "$HOME/Library/MobileDevice/Provisioning Profiles"

      # 7) Bygg → arkivera → exportera IPA
      - name: Build .ipa (archive + export)
        script: |
          set -e
          test -n "$PROVISIONING_PROFILE_UUID" || { echo "❌ Missing PROVISIONING_PROFILE_UUID"; exit 1; }

          echo ">>> Clean"
          xcodebuild clean \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' | xcbeautify

          echo ">>> Show key build settings (pre-archive)"
          xcodebuild -project "$XCODE_PROJECT" -scheme "$XCODE_SCHEME" -showBuildSettings | egrep 'PRODUCT_BUNDLE_IDENTIFIER|DEVELOPMENT_TEAM|CODE_SIGN_STYLE|CODE_SIGN_IDENTITY|PROVISIONING_PROFILE_SPECIFIER' || true

          echo ">>> Archive"
          set -o pipefail
          xcodebuild archive \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -archivePath "build/ios/${IPA_NAME}.xcarchive" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="${CODESIGN_ID}" \
            DEVELOPMENT_TEAM="${TEAM_ID}" \
            PRODUCT_BUNDLE_IDENTIFIER="${BUNDLE_ID}" \
            PROVISIONING_PROFILE_SPECIFIER="${PROVISIONING_PROFILE_UUID}" \
            | tee xcodebuild_archive.log | xcbeautify

          echo ">>> Write export options to file"
          cat > export_options.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key><string>development</string>
            <key>teamID</key><string>${TEAM_ID}</string>
            <key>signingStyle</key><string>manual</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${BUNDLE_ID}</key><string>${PROVISIONING_PROFILE_UUID}</string>
            </dict>
            <key>compileBitcode</key><false/>
          </dict>
          </plist>
          PLIST

          echo ">>> Export IPA"
          xcodebuild -exportArchive \
            -archivePath "build/ios/${IPA_NAME}.xcarchive" \
            -exportOptionsPlist export_options.plist \
            -exportPath build/ios/ipa \
            | xcbeautify

          echo "IPA(s) created:"
          ls -la build/ios/ipa || true

      # 8) Verifiera IPA-struktur och säkra Info.plist-fallet
      - name: Inspect and normalize IPA (Info.plist case)
        script: |
          set -e
          IPA_PATH="$(ls build/ios/ipa/*.ipa 2>/dev/null | head -n 1)"
          [ -n "$IPA_PATH" ] || { echo "❌ No IPA to inspect"; exit 1; }
          echo ">>> Inspect original IPA contents (top ~120 lines):"
          unzip -l "$IPA_PATH" | sed -n '1,120p' || true

          WORKDIR="$(mktemp -d)"
          unzip -q "$IPA_PATH" -d "$WORKDIR"
          APP_DIR="$(find "$WORKDIR/Payload" -maxdepth 2 -type d -name "*.app" | head -n 1)"
          if [ -z "$APP_DIR" ]; then
            echo "❌ No .app in IPA"; exit 1
          fi

          if [ -f "$APP_DIR/info.plist" ] && [ ! -f "$APP_DIR/Info.plist" ]; then
            echo ">>> Renaming info.plist -> Info.plist"
            mv "$APP_DIR/info.plist" "$APP_DIR/Info.plist"
            (cd "$WORKDIR" && zip -qry "$IPA_PATH" .)
            unzip -l "$IPA_PATH" | grep -i "Info.plist" && echo "✅ Info.plist ensured"
          else
            echo "Info.plist case already OK"
          fi

          echo ">>> Read CFBundleIdentifier from Info.plist:"
          /usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$APP_DIR/Info.plist" || true

      # 9) Sanity: App Store Connect env
      - name: Sanity check App Store Connect env vars
        script: |
          miss=0
          for v in APP_STORE_CONNECT_ISSUER_ID APP_STORE_CONNECT_KEY_IDENTIFIER APP_STORE_CONNECT_PRIVATE_KEY; do
            if [ -z "${!v}" ]; then echo "MISS: $v is empty"; miss=1; else echo "OK: $v present"; fi
          done
          [ $miss -eq 0 ] || { echo "❌ Fix missing env vars in Codemagic → group 'admin'"; exit 1; }

      # 10) Preflight ASC (visa providers + key header/footer)
      - name: Preflight: verify App Store Connect API access
        script: |
          set -e
          printf "%s" "$APP_STORE_CONNECT_PRIVATE_KEY" > appstore_key.p8
          echo "Key header: $(head -n 1 appstore_key.p8)"
          echo "Key footer: $(tail -n 1 appstore_key.p8)"
          openssl pkey -in appstore_key.p8 -noout || { echo "❌ Invalid private key format"; exit 1; }
          echo ">>> Listing providers (ignore if not needed):"
          xcrun altool --list-providers \
            --apiKey "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --apiIssuer "$APP_STORE_CONNECT_ISSUER_ID" || true

      # 11) Upload till TestFlight (endast logg-tillägg)
      - name: Upload to TestFlight (altool, verbose JSON)
        script: |
          set -e
          IPA="$(ls build/ios/ipa/*.ipa 2>/dev/null | head -n 1)"
          [ -n "$IPA" ] || { echo "❌ No IPA found to upload"; exit 1; }

          echo ">>> Uploading with altool (JSON output)"
          if [ -n "$ASC_PROVIDER" ]; then
            xcrun altool --upload-app \
              --file "$IPA" \
              --type ios \
              --apiKey "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
              --apiIssuer "$APP_STORE_CONNECT_ISSUER_ID" \
              --asc-provider "$ASC_PROVIDER" \
              --output-format json | tee altool_upload.json
          else
            xcrun altool --upload-app \
              --file "$IPA" \
              --type ios \
              --apiKey "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
              --apiIssuer "$APP_STORE_CONNECT_ISSUER_ID" \
              --output-format json | tee altool_upload.json
          fi

          echo ">>> altool raw result saved → altool_upload.json"
          cat altool_upload.json || true

    artifacts:
      - build/ios/ipa/*.ipa
      - build/ios/*.xcarchive
      - export_options.plist
      - altool_upload.json
