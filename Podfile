platform :ios, '15.0'

# ←←← THIS LINE IS THE ONLY ONE WE ADD
project 'TryggSamtalApp.xcodeproj'

target 'TryggSamtalApp' do
  use_frameworks!

  # You can add real pods later (Alamofire, etc.) – empty is fine for now
end
